import { TbPlayerSkipForward } from "react-icons/tb";
type Props = {
  size?: number;
};
export default function PlayerSkipForwardIcon({ size }: Props) {
  return <TbPlayerSkipForward size={size} />;
}
