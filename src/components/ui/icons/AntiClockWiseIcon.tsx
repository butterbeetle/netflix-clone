import { GiAnticlockwiseRotation } from "react-icons/gi";
type Props = {
  size?: number;
};
export default function AntiClockWiseIcon({ size }: Props) {
  return <GiAnticlockwiseRotation size={size} />;
}
